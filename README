CPE 453 – Assignment 05
Partners:
    Sreerenjini Surendran Namboothiri (snamboot)
    Katie Slobodsky (kslobods)
    Juan E. Cisneros (jcisne23)

Description
-----------
Implements the `minls` and `minget` utilities for MINIX v3 filesystem
images. Both programs use shared code in `minix_fs.c` for partition
handling, superblock loading, inode lookup, directory traversal, and
file reading (including holes and indirect blocks).

Files
-----
Makefile       – builds minls and minget with required flags  
minix_fs.h     – shared definitions and prototypes  
minix_fs.c     – shared filesystem logic  
minls.c        – lists directories or file metadata  
minget.c       – extracts regular files to stdout or a specified file

Build
-----
    make
    ./minls
    ./minget

Clean:
    make clean
